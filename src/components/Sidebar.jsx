import {Sparkles,History,Settings,PanelLeftClose,PanelLeftOpen} from 'lucide-react';
export default function Sidebar({tools,activeTool,onSelect,collapsed,setCollapsed,mobileOpen,setMobileOpen}){return <><button className="mobile-menu" onClick={()=>setMobileOpen(true)}><PanelLeftOpen size={20}/></button><aside className={`sidebar ${collapsed?'collapsed':''} ${mobileOpen?'mobile-open':''}`}><div className="brand-row"><div className="brand-mark">N</div>{!collapsed&&<div><strong>NOVA</strong><small>AI Studio</small></div>}<button className="ghost-icon desktop-collapse" onClick={()=>setCollapsed(!collapsed)}>{collapsed?<PanelLeftOpen size={18}/>:<PanelLeftClose size={18}/>}</button><button className="ghost-icon mobile-close" onClick={()=>setMobileOpen(false)}>×</button></div><nav><p className="nav-label">{!collapsed&&'Workspace'}</p>{tools.map(t=><button key={t.id} className={`nav-button ${activeTool===t.id?'active':''}`} onClick={()=>{onSelect(t.id);setMobileOpen(false)}}><span>{t.icon}</span>{!collapsed&&t.name}</button>)}<button className="nav-button" onClick={()=>onSelect('history')}><History size={18}/>{!collapsed&&'History'}</button></nav><div className="sidebar-bottom"><div className="usage-card"><Sparkles size={18}/>{!collapsed&&<div><strong>Demo workspace</strong><small>Secure live AI is optional.</small></div>}</div><button className="nav-button" onClick={()=>onSelect('settings')}><Settings size={18}/>{!collapsed&&'Settings'}</button></div></aside>{mobileOpen&&<button className="mobile-overlay" onClick={()=>setMobileOpen(false)}/>}</>}
