import{Clock3,Trash2}from'lucide-react';export default function HistoryView({history,onClear,onOpen}){return <section className="page-section"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>Generation history</h1><p>Saved locally in this browser.</p></div><button className="secondary-button" onClick={onClear} disabled={!history.length}><Trash2 size={17}/>Clear history</button></div>{!history.length?<div className="empty-state"><Clock3 size={34}/><h2>No generations yet</h2><p>Your completed outputs will appear here.</p></div>:<div className="history-grid">{history.map(i=><article key={i.id} className="history-card"><div><span>{i.toolName}</span><time>{new Date(i.createdAt).toLocaleString()}</time></div><h3>{i.title}</h3><p>{i.output.slice(0,170)}{i.output.length>170?'…':''}</p><button onClick={()=>onOpen(i)}>Open result</button></article>)}</div>}</section>}
