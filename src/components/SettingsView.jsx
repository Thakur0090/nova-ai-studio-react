export default function SettingsView({mode,setMode}){return <section className="page-section"><div className="page-heading"><div><p className="eyebrow">Workspace</p><h1>Settings</h1><p>Choose how NOVA generates content.</p></div></div><div className="settings-grid"><article className="settings-card"><h2>Generation mode</h2><label className={`mode-option ${mode==='demo'?'selected':''}`}><input type="radio" checked={mode==='demo'} onChange={()=>setMode('demo')}/><div><strong>Portfolio demo mode</strong><p>Works immediately with local generators.</p></div></label><label className={`mode-option ${mode==='live'?'selected':''}`}><input type="radio" checked={mode==='live'} onChange={()=>setMode('live')}/><div><strong>Live AI mode</strong><p>Uses the secure Netlify function and requires OPENAI_API_KEY.</p></div></label></article><article className="settings-card"><h2>Privacy and security</h2><p>API keys never appear in React code or GitHub. History is stored only in this browser.</p></article><article className="settings-card"><h2>Project positioning</h2><p>Demonstrates React components, state, persistence, exports, responsive UI, and optional serverless integration.</p></article></div></section>}
